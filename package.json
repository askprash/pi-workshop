{
  "name": "pi-workshop-extension",
  "version": "0.2.0-safe-beta",
  "description": "Safe-beta Pi workshop extension for recursive expert ideation with audit artifacts.",
  "keywords": ["pi-package", "pi-extension", "workshop", "agents"],
  "license": "MIT",
  "author": "Prashanth Prakash",
  "//repository_TODO": "Update repository URL before publishing — no git remote was configured at the time this manifest was generated.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prashanthprakash/pi-workshop-extension.git"
  },
  "bugs": {
    "url": "https://github.com/prashanthprakash/pi-workshop-extension/issues"
  },
  "homepage": "https://github.com/prashanthprakash/pi-workshop-extension#readme",
  "type": "module",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node tests/static-tests.mjs",
    "smoke": "pi -e ./index.ts --mode json --no-session -p \"/workshop-config --check\" >/tmp/pi-workshop-smoke.jsonl",
    "validate": "npm test && npm run smoke"
  },
  "//peerDependencies_TODO": "Pin to real version ranges before publishing. Only @earendil-works/pi-tui was discoverable locally (0.74.2); others left as '*' until verified.",
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": ">=0.74.0",
    "typebox": "*"
  },
  "engines": {
    "node": ">=20"
  }
}
